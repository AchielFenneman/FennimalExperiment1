# Dropbox conflicted copies (2026-08-26)

Five `LAPTOP-KJCL1A4A's conflicted copy 2026-08-26` files sat next to the live originals. Compared 2026-09-03 before deletion.

| File | Result |
|---|---|
| `1_General_Parameters.js` | Original is a strict superset (gist descriptions, live MorphTask tunables). Conflict unique lines were leftover developing-photo jumble-slot numbers. |
| `2_Stimulus_data.js` | Conflict still used the old hat-binding `hub` / `arms` / `fillers` / `hop_catch_after_errors` authoring API. Live file already moved to `condition` + join blocks. |
| `4_MorphTask.js` | Conflict had the older jumble-canvas layout (`_jumbleCanvasBox`) and hat-only identity keys. Live file is the two-spot polaroid + `response_key_icons` / `show_head_on_prime` version. |
| `index.html` | Conflict only differed in older cache-bust query strings. Live file has Firebase session Layer 1 plus the current script list. |
| `INTERACTION_AND_PHASE_TYPES.md` | Conflict documented the old hub/arms binding graph. Live doc already describes the current condition/join flow. |

Nothing unique in the conflicted copies needed merging. Deleted after this check.
